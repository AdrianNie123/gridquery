# Data artifacts: rebuilt via `make land`, never committed
data/

# Python
.venv/
__pycache__/
*.pyc

# Secrets: API keys live in .env, never committed (.env.example documents the shape)
.env

# dlt runtime state
.dlt/secrets.toml
transform/target/
transform/dbt_packages/
transform/logs/
logs/
