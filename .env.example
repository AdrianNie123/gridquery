# Copy to .env and fill in. The .env file is gitignored; never commit a real key.
# The key funds the Phase 4 NL translation layer (claude-haiku-4-5) only.
# Set a monthly spend cap in the Anthropic console. The app runs local-only.
ANTHROPIC_API_KEY=
